# Notebooks

This folder contains lightweight demonstration notebooks.

The notebooks are not the full experimental protocol. They are meant to show
the project pipeline in a compact way:

1. load a selected trained checkpoint;
2. optionally rerun a small training job;
3. run the evaluation suite;
4. plot training and evaluation results;
5. optionally open the diagnostics UI.
